export enum MathReservationRoutes {
  genInfo = 'gen-info',
  genReservationInterfaces = 'gen-reservation-interfaces',
  feedbackProcess = 'feedback-process',
  closingResults = 'closing-results',
  accountingReconciliation = 'accounting-reconciliation',
  closingReports = 'closing-reports',
}
